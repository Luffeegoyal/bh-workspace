name: kDrive v3 final gated symlink probe

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  final-gated-probe:
    runs-on: ubuntu-latest
    env:
      KDRIVE_TOKEN: ${{ secrets.KDRIVE_TOKEN }}
    steps:
      - name: Install dependencies
        run: python -m pip install --quiet requests xxhash
      - name: Controlled validator probe
        run: echo "Manual-only. Historical probe retained for evidence; do not run unless a fresh scoped validation is needed."
